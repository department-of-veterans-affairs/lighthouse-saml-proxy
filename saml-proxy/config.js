// This file used to be used to configure the IDP attribute metadata. That was
// removed but this configuration shell is being retained because it is likely
// that we will need to introduce IDP configuration parameters again in the
// future.

module.exports = {}
